const home = "/";
const login = "/login";
const register = "/register";
const productDetail = "/product/:id";
const checkout = "/checkout";

const routes = {
  home,
  login,
  register,
  productDetail,
  checkout,
};

export default routes;